import React from 'react';
import ReactDOM from 'react-dom';
import SearchContainer from './components/searchbar_with_filter/SearchContainer';

ReactDOM.render(
  <SearchContainer />,
  document.getElementById('search-component')
);
